# UI 风格规范

视觉目标：轨道交通运营管理后台 + 路演演示模式。

应当：

- 使用深蓝、铁灰、白色为主色，青色用于状态和关键动作点缀。
- 减少大面积蓝紫霓虹渐变和空洞科技装饰。
- 增加真实业务界面质感：表格、筛选、流程步骤、审批状态、风险标签、数据来源、操作日志。
- 首页、登录页、OPC页、路演模式页预留真实图片区域。
- 图片必须来自本地 `public/assets/real/`，缺失时显示内置 CSS/SVG 占位。

不应当：

- 使用外部网络图片。
- 使用真实机构 Logo。
- 放置扫码入口。
- 把每个页面做成宣传卡片堆叠，而忽略操作流和数据状态。
