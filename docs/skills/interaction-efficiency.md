# 交互效率规范

系统操作要适合路演，也要像真实软件：

- 核心流程支持一键演示和分步引导。
- 批量操作必须有状态提示。
- 错误原因应能在表格中展开查看。
- AI 建议必须支持调度员确认。
- 报告页支持导出 TXT、JSON、Markdown。
- 全局保留 QuickDemoPanel，便于快速跳转和触发演示动作。
- 全局保留 AI 调度助手入口，按当前页面上下文给出建议。

避免用户在多个页面来回找按钮。每个核心页面都应有清晰的下一步动作或结果反馈。
